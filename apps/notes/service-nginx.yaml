apiVersion: v1
kind: Service
metadata:
  name: notes-nginx
  namespace: notes
spec:
  type: NodePort
  selector:
    app: notes-nginx
  ports:
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30515
